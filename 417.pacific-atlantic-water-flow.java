class Solution {
    public List<int[]> pacificAtlantic(int[][] matrix) {
        int m = matrix.length;
        int n = matrix[0].length;
        int temp[][]=new int[m][n];
        for (int )
    }
}
